generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model Admin {
  id Int   @default(autoincrement()) @id
  full_name String
  username String
  passwird String
}